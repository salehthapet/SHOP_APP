import 'package:flutter/material.dart';

class Favirotscreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Favirotscreen')),
    );
  }
}
